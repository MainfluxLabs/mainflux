services:
  broker:
    image: nats:2.10-alpine
    command: "-c /etc/nats/nats.conf"
    volumes:
      - ./../nats/:/etc/nats
      - mainfluxlabs-broker-volume:/data
    ports:
      - ${MF_NATS_PORT}:${MF_NATS_PORT}
